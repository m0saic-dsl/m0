// M0_VALIDATION_ERROR_SPECS and makeValidationError are internal helpers —
// imported directly from "./errors" by dsl internals, not part of the public API.
export type {
  M0ValidationErrorKind,
  M0ValidationErrorCode,
  M0ValidationError,
  M0ValidationResult,
} from "./errors";
