{
    "name": "m0-monorepo",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build -w packages/dsl && npm run build -w packages/dsl-file-formats && npm run build -w packages/dsl-stdlib && npm run build -w packages/dsl-visual-tests",
        "test": "npm run test -w packages/dsl && npm run test -w packages/dsl-file-formats && npm run test -w packages/dsl-stdlib",
        "test:slow": "npm run test:stress -w packages/dsl",
        "lint": "eslint ."
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "@types/node": "^25.0.3",
        "jest": "^30.0.2",
        "ts-jest": "^29.4.9",
        "typescript": "^6.0.2"
    }
}
