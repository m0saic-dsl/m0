{
  "name": "m0",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/dsl && npm run build -w packages/dsl-file-formats && npm run build -w packages/dsl-stdlib",
    "test": "npm run test -w packages/dsl && npm run test -w packages/dsl-file-formats && npm run test -w packages/dsl-stdlib",
    "test:slow": "npm run test:stress -w packages/dsl"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "jest": "30.0.2",
    "ts-jest": "^29.4.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "sharp": "^0.35.3",
    "@resvg/resvg-wasm": "^2.6.2"
  }
}
